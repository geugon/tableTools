Tool for performing arbitrary manipulations upon tabulated data.  Intended to provide mechanisms to select data subsets, peform arbitrary column operations, and build additional enteries based on existing data.  Little to no format flexibility intended.

To run, use python3

No additional dependancies
